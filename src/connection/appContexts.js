import { createContext } from 'react';

export const UserProfile = createContext();
export const Session = createContext();

export const Lands = createContext();
export const Members = createContext();